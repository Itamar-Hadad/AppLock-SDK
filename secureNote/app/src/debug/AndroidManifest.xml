<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: permits plaintext HTTP so the demo can reach a local dev server over LAN
     (see demo/README.md, "pointing at a local dev server"). Never merged into a release build. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>